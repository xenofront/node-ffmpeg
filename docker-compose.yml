version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: base
    container_name: ffmpeg
    ports:
      - "3355:3355"
    volumes:
      - ./src:/var/www/src:ro
    command: npm run dev
